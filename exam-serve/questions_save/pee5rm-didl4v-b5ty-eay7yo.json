{"questions_stem":"题目 有一个 input 查询框，每一次 change 都会触发一个“ a 函数”\n要求\n1.此 input 框的 change 请求 在间隔 0.3s 内的过滤掉\n2.提供一个包装函数，可以对上述的“ a 函数”请求过滤做统一的封装\n```\n<input id=\"input\" />\n```","questions_answer":"```\n<input id=\"input\" oninput=\"handleInput()\"/>\n\nlet timer = null;\nfunction debounce(fn, delay = 2000) {\n    return function () { \n        var context = this;\n        var args = arguments;\n        timer && clearTimeout(timer)\n        timer = setTimeout(function () {\n            fn.apply(context, args);\n        }, delay);\n    }\n}\nfunction handleInput(){\n    var value = document.getElementById(\"input\").value;\n    debounce(requestA,300)(value);\n}\nfunction requestA(value) {\n    console.log('click',value)\n}\n```","questions_id":"pee5rm-didl4v-b5ty-eay7yo"}